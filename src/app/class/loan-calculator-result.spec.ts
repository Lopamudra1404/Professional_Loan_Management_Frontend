import { LoanCalculatorResult } from './loan-calculator-result';

describe('LoanCalculatorResult', () => {
  it('should create an instance', () => {
    expect(new LoanCalculatorResult()).toBeTruthy();
  });
});
