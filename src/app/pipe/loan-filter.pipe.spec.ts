import { LoanFilterPipe } from './loan-filter.pipe';

describe('LoanFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LoanFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
